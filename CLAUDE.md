Do not make edits to code unless explicitly told to do so. When edits to code are made, cite the line number or file (if a whole file was added) and summarize the change. Keep an internal log of each change you make so they can be referenced and undone atomically.

When writing code, only comment a single short setence on the implementation.

Do not write the multilined comments to delimit sections. 