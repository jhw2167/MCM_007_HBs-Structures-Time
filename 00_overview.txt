Overview
---

Keep Notes:
---

Structures over Time

- make a copy of the over world "canvas"
- track each structure that spawns in the over world, ad them to manager ::managed chunk extension
- save start and end server tick

- hack structure creation method - no structures will spawn in canvas, but we need same biome distribution
- must be a dynamic copy of the over world, so all modded configurations are transfered
- structureManager.isApproved()
- load json config 
- try to upgrade villages to integrated villages
- remove all integrated villages
- spawn normal villages
- spawn integrated ones on command
- 

I want to make an exact copy of the Minecraft Overworld, it's structures, terrain patterns and layout. The vanilla_dimension for a given Minecraft seed will have each chunk identical to the corresponding chunk in the over world. For a given chunk, it will be the same biome as the Overworld, make use of the same carvers, and structure setters. It will have the same ore clumps at the save position, the same trees and geodes. This must be copied at runtime, not data gen'd, so all modded additions to the Overworld are considered. Where can I hook into dynamically to get a link to the Overworlds source generators.

- Overworld copy has all structures removed
- use "do place" structure to place in established location, trigger jigsaws
- 



- if player spends significant time in a chunk, the structure is not upgraded - we'll see here, makes sense for a surprise structure to spawn or villagers to build
- 
Track Players Near Structures - need to know if player has actually discovered structures, more than structures that were just chunk loaded at some point. For when we start random chunk loading to find structures.
- need to start saving client data on client side, we can do json in client folder

